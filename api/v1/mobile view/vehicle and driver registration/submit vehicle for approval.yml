info:
  name: submit vehicle for approval
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/v1/me/vehicles/{{vehicle_id}}/submit"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
