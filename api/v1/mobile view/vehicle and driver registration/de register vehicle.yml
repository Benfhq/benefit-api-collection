info:
  name: de register vehicle
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/v1/corporate/vehicle/{{vehicle_id}}/deregister"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
