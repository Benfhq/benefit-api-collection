info:
  name: get pending tasks
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/v1/me/pending-tasks"
  body:
    type: json
    data: |
      {
        "registration_number": "MP28ZK7656",
        "ownership_type": "company",
        "usage_type": "self_and_company"
      }
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
