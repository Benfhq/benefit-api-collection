info:
  name: download file
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/v1/files/{{file_id}}/download"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

runtime:
  variables:
    - name: file_id
      value: 8817e688-0a24-4fa8-bb1f-8146d1d441e7

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
