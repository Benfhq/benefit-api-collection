info:
  name: get group members
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/v1/corporate/employee-groups/{{group_id}}/members"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
