info:
  name: create employee groups
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/corporate/employee-groups"
  body:
    type: json
    data: |
      {
        "name": "Department Engineering",
        "match_type": "all",
        "option_ids": ["358cb05e-d13d-4b12-befa-78ea7cf8e7b1"]
      }
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
