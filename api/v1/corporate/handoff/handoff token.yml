info:
  name: handoff token
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/auth/handoff"
  body:
    type: json
    data: |-
      {
        "email": "sanket.wadte@pinelabs.com"
      }
  auth:
    type: bearer
    token: "{{handoff_secret}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
