info:
  name: activate user
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/webhook/activate_user"
  auth:
    type: bearer
    token: pinewebhooksecret

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
