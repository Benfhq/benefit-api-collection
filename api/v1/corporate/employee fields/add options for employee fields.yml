info:
  name: add options for employee fields
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/corporate/employee-fields/{{employee_field_id}}/options"
  body:
    type: json
    data: |-
      {
        "name": "Chennai"
      }
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
