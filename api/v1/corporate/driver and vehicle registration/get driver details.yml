info:
  name: get driver details
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/v1/corporate/drivers?status=submitted"
  params:
    - name: status
      value: submitted
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
