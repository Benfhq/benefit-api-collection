info:
  name: vehile registration
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/v1/corporate/vehicles?status=submitted"
  params:
    - name: status
      value: submitted
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
