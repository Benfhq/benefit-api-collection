info:
  name: patch claim
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/api/v1/me/claims/{{claim_id}}"
  body:
    type: json
    data: |
      {
        "file_ids": ["aba53b7a-0509-46dd-921b-20252d8463ab"]
      }
  auth:
    type: bearer
    token: "{{auth_token}}"

runtime:
  variables:
    - name: claim_id
      value: b0c140fd-7d7a-4724-a45e-d207d02c1f5f

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
