info:
  name: get claims to approve
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/v1/corporate/claims"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

runtime:
  variables:
    - name: claim_id
      value: b0c140fd-7d7a-4724-a45e-d207d02c1f5f

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
