info:
  name: get claims
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/me/claims"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
