info:
  name: get all policies
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/corporate/benefit-policies"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
