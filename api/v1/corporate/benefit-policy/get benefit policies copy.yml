info:
  name: get benefit policies copy
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/corporate/benefits/{{benefit_id}}/policies"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
