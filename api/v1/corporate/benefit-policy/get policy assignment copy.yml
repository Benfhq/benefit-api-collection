info:
  name: get policy assignment copy
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/corporate/benefit-policies/{{policy_id}}/assignments"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
